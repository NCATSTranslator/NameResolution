<?xml version="1.0" encoding="UTF-8"?>
<!--
  Reference schema for NameRes.

  This file documents the expected schema. The authoritative schema in a
  deployed NameRes instance is generated by the Schema API calls in
  setup_solr.sh during data loading, which writes the complete field
  definitions into name_lookup/conf/managed-schema.xml inside the core
  directory.

  Field types:
    LowerTextField  - standard tokenizer + lowercase filter (used for names, preferred_name)
    exactish        - keyword tokenizer + lowercase filter (used for exact matching)

  Fields:
    names               LowerTextField  multivalued  - synonym list
    names_exactish      exactish        multivalued  - copy of names for exact matching
    curie               string                       - CURIE identifier
    preferred_name      LowerTextField               - preferred display name
    preferred_name_exactish  exactish               - copy of preferred_name for exact matching
    types               string          multivalued  - biolink types
    shortest_name_length pint                        - length of shortest synonym
    curie_suffix        plong           docValues    - numeric suffix of CURIE (for sorting)
    taxa                string          multivalued  - taxon identifiers
    taxon_specific      boolean                      - whether entity is taxon-specific
    clique_identifier_count pint                     - number of identifiers in clique

  Copy fields:
    names -> names_exactish
    preferred_name -> preferred_name_exactish
-->
<schema name="name_lookup" version="1.6">
  <uniqueKey>id</uniqueKey>
</schema>
